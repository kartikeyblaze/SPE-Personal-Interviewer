apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: promtail
rules:
  - apiGroups: [""]
    resources:
      - pods
      - nodes
      - nodes/proxy
      - services
      - endpoints
    verbs: ["get", "list", "watch"]
